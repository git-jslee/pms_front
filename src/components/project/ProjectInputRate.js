import React from 'react';

// return <span>{JSON.stringify(returnData)}</span>;

const ProjectInputRate = ({ inputRate }) => {
  return (
    <>
      <div>
        <span>{JSON.stringify(inputRate)}</span>;
      </div>
    </>
  );
};

export default ProjectInputRate;
